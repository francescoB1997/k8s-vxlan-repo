apiVersion: v1
kind: Pod
metadata:
  name: podA-node1
  labels:
    app: test-ping
    zone: group1
spec:
  containers:
  - name: alpine
    image: alpine
    command: ["/bin/sh", "-c", "sleep infinity"]
    securityContext:
      capabilities:
        add: ["NET_RAW"]
  nodeSelector:
    kubernetes.io/hostname: minikube
---
apiVersion: v1
kind: Pod
metadata:
  name: podB-node2
  labels:
    app: test-ping
    zone: group2
spec:
  containers:
  - name: alpine
    image: alpine
    command: ["/bin/sh", "-c", "sleep infinity"]
    securityContext:
      capabilities:
        add: ["NET_RAW"]
  nodeSelector:
    kubernetes.io/hostname: minikube-m02