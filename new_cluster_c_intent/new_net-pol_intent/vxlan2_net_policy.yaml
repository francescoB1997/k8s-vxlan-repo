apiVersion: crd.projectcalico.org/v1
kind: NetworkPolicy
metadata:
  name: allow-group2-internal
  namespace: default
spec:
  selector: "zone == 'group2'"
  types:
  - Ingress
  ingress:
  - action: Allow
    source:
      selector: "zone == 'group2'"